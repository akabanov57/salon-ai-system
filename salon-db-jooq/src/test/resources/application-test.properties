# Тестовая конфигурация модуля salon-db-jooq
db.url=jdbc:h2:mem:salontestdb;MODE=PostgreSQL;DEFAULT_NULL_ORDERING=HIGH
db.username=sa
db.password=
db.dialect=H2
db.pool.max-size=2

# Разрешаем фабрике накатить схему в память H2
db.auto-migrate=true
